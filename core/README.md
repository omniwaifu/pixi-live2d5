# Live2D Cubism Core

This fork uses the Cubism 5 R4 Core runtime and declarations. Download the matching files with:

```sh
bun run setup
```

The setup script extracts `live2dcubismcore.js` and `live2dcubismcore.d.ts` from the official Cubism 5 R4 SDK archive. These downloaded files are intentionally ignored by Git.
